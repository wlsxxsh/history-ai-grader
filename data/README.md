This directory stores local runtime data only.

- `app-state.json` is created on first launch and should not be committed.
- `uploads/` stores imported materials and answer sheets.
- `generated/` stores temporary exports and model debug artifacts.
